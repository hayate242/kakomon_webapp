<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <header>
      <div class="title">
        <h1>過去問共有</h1>
        <p>クリックしたらダウンロードが開始されます</p>
      </div>
    </header>

  <div class="main_flame">

    <div class="univ_flame">
      <div class="univ_name">
        <h3><%= @university + "　　" + @department + "　　" + @major %></h3>
      </div>
    </div>

    <div class="list_flame">
      <ul>
        <% @contents.each do |content| %>
        <li><%= link_to(content.lesson + " " + content.year.to_s + "年", action: "download", id: content.id) %>
        <% end %>
      </ul>
    </div>

  </div>

  </body>
</html>
